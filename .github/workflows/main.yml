name: Deploy to Heroku
 
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: "booking-app-joineando" #Must be unique in Heroku
          heroku_email: "fabiola.robles1@upr.edu"
          appdir: "backend" # <- This will point to the api folder in your project
        env:
          HD_DB_HOST: ec2-54-208-96-16.compute-1.amazonaws.com
          HD_DB_NAME: d24bjj94mias6u
          HD_DB_USER: uyafdrasmnoyub
          HD_DB_PASSWORD: 95295805e41d44306c87d15310633067c12dfd9c0529e300b84e4b28e6adaedb
          HD_DB_PORT: 5432
        
